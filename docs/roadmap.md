# Дорожная карта SabaiStay

Принцип: сначала всё **бесплатное**, платное подключаем по мере бюджета. Telegram —
надёжный костяк (дёшево/бесплатно), Facebook — главный по объёму, но платный и хрупкий.

## Фаза 0 — каркас (сейчас, без денег) ✅ в работе
- [x] Структура проекта, README, CLAUDE.md
- [x] Архитектура (`docs/architecture.md`)
- [x] Модель данных (`db/schema.sql`)
- [x] Схема парсера + промпт (`parser/`)
- [x] Газеттир районов (`data/districts.json`)
- [x] Бесплатный Telegram-сборщик через `t.me/s` (`ingest/telegram_preview.py`) — **проверен на живых данных**
- [x] Имя выбрано: **SabaiStay** (`docs/naming.md`); домен — проверить позже
- [x] Найдены источники (`data/sources.md`): `koh_phangan_rent` работает бесплатно (**22 поста собрано** → `data/sample_tg.json`); крупные каналы (`rent_phangan` и др.) с отключённым превью → нужен Telethon
- [x] Разобрать 22 поста в фильтруемые поля (вручную как демо до API-ключа) → `data/sample_listings.json`: **17 объявлений, 5 мусора отсеяно**. Добавлен флаг `is_listing` в схему.
- [x] Нормализатор районов (`parser/normalize_district.py`) — 11/11 на реальных написаниях, exact+fuzzy
- [x] Валидатор + **демо фильтра** (`parser/validate_sample.py`) — фильтр по цене/спальням/району/бассейну работает на живых данных со ссылками на оригинал
- [ ] Подключить API-ключ Claude → автопарсинг новых постов (заменить ручной шаг) — Владимир сделает проект платным
- [ ] Импортировать FB-дамп RW `fb_bulk_2026-05-30.html` как ещё примеры

## Фаза 1 — Telegram живой (бесплатно/почти) 
- [ ] Бэкфилл истории `t.me/s/rent_phangan` (десятки объявлений сразу, без аккаунта)
- [ ] Найти ещё 3-5 каналов/чатов аренды Пхангана
- [ ] Поднять Postgres (Neon free tier)
- [ ] Пайплайн: сбор → Claude-парсер → нормализация → база
- [ ] **Платный шаг (~$5/мес):** Telethon userbot 24/7 на VPS + отдельная SIM для realtime

## Фаза 2 — Витрина (бесплатно)
- [x] **Демо-витрина** `web/index.html` (генератор `web/build.py`): сплит-вид карточки+карта Leaflet, фильтр по району/цене/спальням/типу/бассейну, **сортировка** (дешевле/дороже/свежее), **карточка с несколькими источниками** (дедуп → «🔗 N источников», иконки TG/FB), цена с учётом периода (/мес·/ночь). Самодостаточная (данные вшиты, без npm/бэкенда) — открывается двойным кликом. 17 объектов, 13 на карте.
- [ ] Позже (опционально): пересесть на полированную `/listings` из RW (форк, вырезать amoCRM/данные RW) — когда нужен серверный фильтр на тысячах объектов и сохранённые поиски
- [x] UX-ядро фильтра: **делимая ссылка на поиск** (состояние в URL-хэше), **чипы активных фильтров** + счётчик, **мульти-фильтр удобств** (бассейн/море/wifi/кухня/кондей), доступность (фокус, prefers-reduced-motion)
- [x] Бренд: лого «закат над водой» (`web/brand.svg`), фавикон (инлайн data-URI), шрифт **Baloo 2** под «сабай»-вайб, OG/Twitter-мета + постер `web/og.svg` (на деплое: домен + экспорт PNG)
- [x] Готовность к публикации (PDPA): страница `web/privacy.html` (что храним / убрать объявление ≤30 дней) + подвал витрины. Домен: `.com` занят; `.co/.io/.app/.fun` свободны (подтвердить у регистратора)
- [ ] Деплой на Vercel (hobby — бесплатно), домен `sabaistay.*` (~$10/год), задать `SITE_URL` в `web/build.py` + экспорт `og.png`

## Фаза 3 — Facebook (платно, главный источник)
> ⚠️ Дамп RW `fb_bulk_2026-05-30.html` — это НЕ внешние FB-объявления, а bulk-постер RW (10 объектов RW-xxxx для выкладки в Marketplace). В нейтральный SabaiStay не импортировать (это связало бы продукт с RW). Реальный внешний FB — только через Apify ниже.
- [ ] Завести «расходный» FB-аккаунт (не личный), прогреть, вступить в группы аренды Пхангана
- [ ] **Платно (~$30-60/мес):** Apify managed-актор, публичные группы + Marketplace, расписание
- [ ] **Платно (~$30-50/мес):** резидентный тайский прокси для закрытых групп (cookie-сессия)
- [ ] Мониторинг поломок (пуш в TG если прогон вернул 0/мусор)

## Фаза 4 — Киллер-фича и качество
- [x] Движок сопоставления фильтра `parser/filter_match.py` (общий с витриной, тест пройден): критерии = `saved_filter.criteria`, null-поля не пушим
- [x] Бот «подписка на фильтр» `bot/subscribe_bot.py`: разбор запроса на естественном языке + **кнопочный билдер `/new`** (инлайн-клавиатура: район/цена/спальни/удобства/режим), **дайджест-режим** (instant-пуш или сводка раз в день, `send_digests()`), команды /subscribe /myfilters /unsubscribe, офлайн-демо `--demo`. Ждёт токен @BotFather + поток из пайплайна (`notify_new_listings()`)
- [x] Кросс-источниковый дедуп — текстовый слой `parser/dedup.py` (сигнатура: район+спальни+тип+цена±12%+заголовок, union-find с блокингом по району; тест: TG+FB склеиваются, разные дома — нет). Группировка «N источников» встроена в витрину. pHash-слой по фото — позже (платно, когда потекут фото FB)
- [x] Свежесть/авто-expire `parser/freshness.py` (тест пройден): возраст → active/rented/expired (порог 45 дн), бейджи свежести на карточках, тумблер «скрыть неактуальные», затемнение старых пинов. В демо точка отсчёта = дата последнего скрейпа («снимок»); в проде — реальное now + last_seen

## Фаза 5 — Сравнение с рынком (Airbnb/Booking)
- [x] Движок бенчмарка `parser/benchmark.py` (тест 4/4) + газеттир медиан `data/market_benchmark.json` (двухслойно: медиана ฿/мес долгосрочной аренды по спальням + справочно Airbnb/Booking ฿/ночь)
- [x] На карточке витрины бейдж «−12% / +36% к рынку» + строка «🏨 Airbnb здесь ~N฿/ночь» (12/17 объявлений)
- [x] Таблица `market_benchmark` в `db/schema.sql`; скелет сбора `ingest/market_airbnb_booking.py`
- [ ] Сейчас медианы — **сид-оценки**. Заменить реальными: подключить сбор Airbnb/Booking (платно, как FB) или перенести коллекторы RW `rental-market-ci`

## Заготовки под платную фазу (готовы, проверены ревью)
- [x] `parser/parse.py` — реальный парсинг через Claude API (structured outputs + Batch −50%); ждёт API-ключ
- [x] `ingest/telegram_telethon.py` — userbot 24/7 (резолв сущностей, дедуп, анти-флуд, graceful shutdown); ждёт аккаунт+VPS
- [x] `ingest/fb-setup-checklist.md` — пошаговая настройка Apify+прокси+burner; ждёт бюджет
- [x] Адверсариальное ревью заготовок применено (2 HIGH Telethon + валидации parse.py)

## Что стоит денег (сводка)
| Статья | ~Цена/мес | Когда |
|---|---|---|
| VPS под Telethon | $5 | Фаза 1 (realtime) |
| Apify (FB-актор) | $30-60 | Фаза 3 |
| Резидентный прокси | $30-50 | Фаза 3 |
| Claude-парсинг | <$5 | Фаза 1+ |
| Vercel / Neon | $0 (free) | весь старт |
| Домен | ~$10/год | Фаза 2 |
| **Итого активной фазы** | **~$70-150/мес** | с Фазы 3 |

## Холодный старт до ~100 объявлений
Бэкфилл `t.me/s/rent_phangan` (бесплатно, десятки) + публичные FB-группы через актор +
ручной импорт дампа RW `fb_bulk_2026-05-30.html`. Комбинация даёт сотню быстро.

## Открытые вопросы
- Финальный фокус: **помесячная аренда экспатам** vs **посуточно туристам** (влияет на дефолтные фильтры и парсер). Текущий уклон — помесячная (туда заточен rent-фильтр RW).
- Финальное имя/домен.
